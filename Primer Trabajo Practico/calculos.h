int suma(int,int);
/** \brief Se ingresan dos datos para sumarlos de tipo INT.
 *
 * \param Suma las dos variables ingresadas y las guarda en: resultado.
 * \param Las muestra por pantalla cuando el usuario ingresa 4.
 * \return Devuelve el resultado.
 *
 */
 int resta(int,int);
/** \brief Se ingresan dos datos para restarlos de tipo INT.
 *
 * \param Resta las dos variables ingresadas y las guarda en: resultado.
 * \param Las muestra por pantalla cuando el usuario ingresa 4.
 * \return Devuelve el resultado.
 *
 */
 int multiplicacion(int,int);
/** \brief Se ingresan dos datos para multiplicarlos de tipo INT.
 *
 * \param Multiplica las dos variables ingresadas y las guarda en: resultado.
 * \param Las muestra por pantalla cuando el usuario ingresa 4.
 * \return Devuelve el resultado.
 *
 */
 int division(int,int);
/** \brief Se ingresan dos datos para dividirlos de tipo INT.
 *
 * \param Divide las dos variables ingresadas y las guarda en: resultado.
 * \param Si el usuario ingresa un 0 en el segundo operando. Le dira que no se puede dividir por 0.
 * \param Las muestra por pantalla cuando el usuario ingresa 4.
 * \return Devuelve el resultado.
 *
 */
 int factorialX(int);
/** \brief Se ingresa un dato para sacar el factorial de tipo INT.
 *
 * \param Saca el factorial del primer operando y la guarda en: x
 * \param Si el valor es menor a 0. Le dira que no se puede sacar el factorial de un numero negativo.
 * \param Las muestra por pantalla cuando el usuario ingresa 4.
 * \return Devuelve 0.
 *
 */
  int factorialY(int);
/** \brief Se ingresa un dato para sacar el factorial de tipo INT.
 *
 * \param Saca el factorial del segundo operando y la guarda en: y
 * \param Si el valor es menor a 0. Le dira que no se puede sacar el factorial de un numero negativo.
 * \param Las muestra por pantalla cuando el usuario ingresa 4.
 * \return Devuelve 0.
 *
 */
